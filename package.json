{
  "name": "car-rental-platform",
  "version": "0.1.0",
  "private": true,
  "description": "White-label car rental platform (mobile + web + backend)",
  "scripts": {
    "backend": "npm --prefix backend run dev",
    "web": "npm --prefix apps/web run dev",
    "mobile": "npm --prefix apps/mobile run start",
    "install:all": "npm --prefix backend install && npm --prefix apps/web install && npm --prefix apps/mobile install",
    "dev": "concurrently -n backend,web -c blue,green \"npm run backend\" \"npm run web\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
